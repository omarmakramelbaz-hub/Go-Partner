name: GO Partner Web View

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.0'
          channel: stable
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Configure Flutter Web platform
        run: flutter create . --platforms web --no-pub

      - name: Verify GO Partner brand assets
        shell: bash
        run: |
          set -euo pipefail
          test -s assets/svg/go_partner_logo.svg
          grep -q 'GO' assets/svg/go_partner_logo.svg
          grep -q 'Partner' assets/svg/go_partner_logo.svg
          grep -q 'assets/svg/go_partner_logo.svg' lib/view/layout/auth/screen/login_screen.dart
          grep -q 'assets/svg/go_partner_logo.svg' lib/view/layout/screen/splash_screen.dart

      - name: Build Flutter Web
        run: flutter build web --release --base-href "/Go-Partner/"

      - name: Verify built HD logo and force fresh preview
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          from pathlib import Path

          build = os.environ.get('GITHUB_SHA', 'dev')[:12]
          root = Path('build/web')
          index = root / 'index.html'
          bootstrap = root / 'flutter_bootstrap.js'
          main_js = root / 'main.dart.js'
          logo = root / 'assets' / 'assets' / 'svg' / 'go_partner_logo.svg'

          if not logo.exists() or logo.stat().st_size == 0:
              raise SystemExit('GO Partner logo missing or invalid in web build')

          if main_js.exists() and bootstrap.exists():
              versioned_name = f'main.dart.{build}.js'
              (root / versioned_name).write_bytes(main_js.read_bytes())
              text = bootstrap.read_text(encoding='utf-8').replace('main.dart.js', versioned_name)
              bootstrap.write_text(text, encoding='utf-8')

          html = index.read_text(encoding='utf-8')
          original = '<script src="flutter_bootstrap.js" async></script>'
          opening = f'''
          <style>
            html, body {{ margin: 0; width: 100%; height: 100%; background: #fff; }}
            #go-partner-opening {{ position: fixed; inset: 0; z-index: 2147483647; background: #fff; display: flex; align-items: center; justify-content: center; }}
            #go-partner-opening img {{ width: min(72vw, 360px); max-height: 48vh; object-fit: contain; display: block; }}
          </style>
          <div id="go-partner-opening"><img src="assets/assets/svg/go_partner_logo.svg?build={build}" alt="GO Partner"></div>
          <script>
          (async function () {{
            try {{
              if ('serviceWorker' in navigator) {{
                const regs = await navigator.serviceWorker.getRegistrations();
                await Promise.all(regs.map((r) => r.unregister()));
              }}
              if ('caches' in window) {{
                const keys = await caches.keys();
                await Promise.all(keys.map((k) => caches.delete(k)));
              }}
            }} catch (_) {{}}
            const script = document.createElement('script');
            script.src = 'flutter_bootstrap.js?build={build}';
            script.async = true;
            document.body.appendChild(script);
            const removeOpening = () => {{ const el = document.getElementById('go-partner-opening'); if (el) el.remove(); }};
            window.addEventListener('flutter-first-frame', removeOpening, {{once: true}});
            setTimeout(removeOpening, 3000);
          }})();
          </script>
          '''
          html = html.replace(original, opening) if original in html else html.replace('</body>', opening + '\n</body>')
          index.write_text(html, encoding='utf-8')
          PY

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload GitHub Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
